import { TorchGameClient } from "@/components/torch-game-client";

export default function TorchPage() {
  return (
    <main className="h-[100dvh] w-full overflow-hidden">
      <TorchGameClient />
    </main>
  );
}
